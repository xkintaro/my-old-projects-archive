const KintaroTitle1 = ({ title }) => {
    return (
        <h1 className="kintaro-title-reset kintaro-title-1">
            {title}
        </h1>
    );
};

const KintaroTitle2 = ({ title }) => {
    return (
        <h2 className="kintaro-title-reset kintaro-title-2">
            {title}
        </h2>
    );
};

const KintaroTitle3 = ({ title }) => {
    return (
        <h3 className="kintaro-title-reset kintaro-title-3">
            {title}
        </h3>
    );
};

export { KintaroTitle1, KintaroTitle2, KintaroTitle3 };